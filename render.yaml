services:
  - type: web
    name: telkom-manual-checker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn render_app:app
    envVars:
      - key: APP_PASSWORD
        value: Luna2023
